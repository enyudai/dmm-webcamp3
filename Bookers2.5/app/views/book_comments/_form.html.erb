  <% if current_user != book.user %>
  
      <%= form_with model: [book, comment], url: book_book_comments_path(book.id), local: false do |f| %>
        <%= f.text_area :comment, rows: '5' %>
        <%= f.submit "送信" %>
      <% end %>
      
  <% end %>    